server:
  port: 8080

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: dummy-route
          uri: https://postman-echo.com
          predicates:
            - Path=/get